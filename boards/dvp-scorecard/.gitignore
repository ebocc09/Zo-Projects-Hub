# Credentials & local state — per-user, never commit.
.connections.json
.admin.json

# Display names learned from ZO-002. Real employee names — a local cache, not
# project data, and the reason this board can show them without an MCP client.
.staff-cache.json

# Uploaded/derived survey data — customer PII, stays local.
data/
*.xlsx
*.csv

# Portable runtime, if built here
runtime/

node_modules/
.DS_Store
Thumbs.db
